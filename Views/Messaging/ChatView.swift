import PhotosUI
import SwiftUI
import FirebaseFirestore
import FirebaseStorage
import FirebaseAuth
import UIKit
import UniformTypeIdentifiers
import AVKit

// This view is removed as it is replaced by ClientChatDetailView and DietitianChatDetailView
// The new views provide role-specific messaging interfaces

// Existing code...

#if DEBUG
// @available(iOS 16.0, *)
// struct ChatView_Previews: PreviewProvider {
//     static var previews: some View {
//         let mockSession = SessionStore.previewStore(isLoggedIn: true, unreadNotifications: 0)
//         // return ChatView(chatId: "preview_chat_id_123")
//         //     .environmentObject(mockSession)
//         //     .preferredColorScheme(.dark)
//     }
// }
#endif